{
  "name": "star-color-local-web",
  "version": "2.4.0",
  "author": "ZephyrFBD",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "build:single": "npm run build && node scripts/build-single.mjs",
    "test:hdr": "node tests/hdr-png-qa.mjs",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "@lumaforge/luma-raw-runtime": "0.1.0"
  },
  "devDependencies": {
    "vite": "7.3.6"
  }
}
